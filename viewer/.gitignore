# viewer/data.js IS committed (Tier 2). The GitHub Actions refresh workflow
# regenerates and commits it so Netlify's /app/ split-file view serves fresh
# data; the standalone HTML embeds the same data for the root shareable view.
# Repo is private, so committing processed campaign data here is intentional.
